<h1><%= @topic.title %> </h1>

<%= link_to "Edit Topic", edit_user_topic_path(current_user, @topic), class: 'btn btn-success' %>
<%= link_to "Delete Topic", [@topic.user, @topic], method: :delete, class: 'btn btn-danger', data: { confirm: 'Are you sure you want to delete this topic?' } %>

  <div>

    <%= render @topic.bookmarks, topic: @topic %>

  </div>

  <div>

    <%= render 'bookmarks/form', bookmark: Bookmark.new, topic: @topic %>

  </div>

</div>
